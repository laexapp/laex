import type{LFEventPublisher,WorkshopRepository}from"../domain/ports";import type{RepairStage}from"../domain/types";const order:RepairStage[]=["Recepción","Diagnóstico","Reparación","Control de calidad","Listo para entrega","Entregado"];
export class WorkshopService{constructor(private repository:WorkshopRepository,private events:LFEventPublisher){}async advance(id:string,next:RepairStage,note:string){const item=await this.repository.findOrder(id);if(!item)throw new Error("Orden no encontrada");if(order.indexOf(next)!==order.indexOf(item.stage)+1)throw new Error("Transición de taller inválida");item.stage=next;item.history.push({stage:next,at:new Date().toISOString(),note});await this.repository.save(item);await this.events.publish({type:"lfprinter.service.stage-changed",version:1,occurredAt:new Date().toISOString(),payload:{orderId:id,stage:next}});return item;}}
