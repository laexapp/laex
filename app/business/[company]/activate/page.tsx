"use client";
import { useState,type FormEvent } from "react";import { useParams,useSearchParams } from "next/navigation";import { KeyRound } from "lucide-react";import "@/modules/business-engine/ui/business-access.css";
export default function Page(){const params=useParams<{company:string}>(),search=useSearchParams(),[password,setPassword]=useState(""),[confirmation,setConfirmation]=useState(""),[notice,setNotice]=useState("");async function submit(event:FormEvent){event.preventDefault();if(password!==confirmation){setNotice("Las contraseñas no coinciden.");return}const response=await fetch(`/api/business-app/${params.company}/session`,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({token:search.get("token"),newPassword:password})}),data=await response.json();if(response.ok){setNotice("Contraseña establecida. Ya puedes iniciar sesión.");setTimeout(()=>window.location.assign(`/business/${params.company}/login`),1200)}else setNotice(data.error==="invalid_or_expired_token"?"El enlace venció o ya fue utilizado.":"No fue posible establecer la contraseña.")}return <main className="business-login-shell"><section><div className="business-login-icon"><KeyRound/></div><p className="business-login-kicker">ACTIVACIÓN SEGURA</p><h1>Establece tu contraseña</h1><p>Este enlace funciona una sola vez y revocará sesiones anteriores.</p><form onSubmit={submit}><label>Nueva contraseña<input required type="password" minLength={12} value={password} onChange={event=>setPassword(event.target.value)}/></label><label>Confirmar contraseña<input required type="password" minLength={12} value={confirmation} onChange={event=>setConfirmation(event.target.value)}/></label>{notice&&<div className="business-login-error">{notice}</div>}<button>Establecer contraseña</button></form></section></main>}
