import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { CompanyExperiencePage } from "@/modules/company-experience/ui/CompanyExperiencePage"; import { resolveCompanyExperience, toPublicExperience, validateExperience } from "@/modules/company-experience/engine"; import "../company-experience-entry.css";
export async function generateMetadata({ params }: { params: Promise<{ company: string }> }): Promise<Metadata> { const definition = resolveCompanyExperience((await params).company); if (!definition) return {}; return { title: definition.experience.seo.title, description: definition.experience.seo.description, robots: { index: !definition.experience.seo.noIndex, follow: !definition.experience.seo.noIndex } }; }
export default async function Page({ params }: { params: Promise<{ company: string }> }) { const definition = resolveCompanyExperience((await params).company); if (!definition || !validateExperience(definition).valid) notFound(); return <CompanyExperiencePage data={toPublicExperience(definition)} />; }