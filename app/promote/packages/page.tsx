import type { Metadata } from "next";
import Link from "next/link";
import { ArrowLeft, Check, X } from "lucide-react";
import { packages } from "@/lab-market-intelligence/data";
import MarketHeader from "@/modules/market-intelligence/components/MarketHeader";
export const metadata:Metadata={title:"Paquetes configurables | LAEX",description:"Servicios de evaluación, verificación, monitoreo, promoción y producción de LAEX.",alternates:{canonical:"/promote/packages"}};
export default function PackagesPage(){return <div className="laex-canvas min-h-screen text-white"><MarketHeader/><main className="mx-auto w-[min(100%-2rem,92rem)] py-16"><Link href="/promote" className="inline-flex items-center gap-2 text-xs text-slate-500"><ArrowLeft size={14}/>Volver</Link><span className="laex-eyebrow mt-10 block">Catálogo comercial configurable</span><h1 className="laex-display mt-6 text-5xl sm:text-7xl">Servicios con límites claros.</h1><p className="mt-6 max-w-3xl text-sm leading-7 text-slate-400">Precios, moneda, impuestos, duración y condiciones se definen en una orden comercial separada. Ningún paquete incluye una puntuación favorable.</p><div className="mt-10 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{packages.map(item=><article key={item.id} className="laex-card flex rounded-3xl p-6"><div className="flex flex-1 flex-col"><span className="text-[11px] font-bold uppercase tracking-[.2em] text-cyan-200">{item.category}</span><h2 className="mt-4 text-xl font-semibold">{item.name}</h2><div className="mt-6 space-y-3">{item.scope.map(scope=><p key={scope} className="flex gap-2 text-xs text-slate-300"><Check size={13} className="shrink-0 text-emerald-300"/>{scope}</p>)}</div><div className="mt-6 border-t border-white/[.06] pt-5">{item.exclusions.map(exclusion=><p key={exclusion} className="mt-2 flex gap-2 text-xs text-slate-600"><X size={13} className="shrink-0 text-rose-300"/>{exclusion}</p>)}</div><Link href="/promote" className="mt-auto pt-7 text-xs font-bold text-cyan-200">Solicitar información →</Link></div></article>)}</div></main></div>;}
