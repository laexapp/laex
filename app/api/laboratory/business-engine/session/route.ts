import { NextRequest,NextResponse } from "next/server";import { ensureLfPrinterPilot } from "@/modules/business-engine/pilots/lf-printer/runtime";import { BUSINESS_SESSION_COOKIE,getLaboratoryBusinessRuntime } from "@/modules/business-engine/server/runtime";
export const runtime="nodejs";
export async function POST(request:NextRequest){try{await ensureLfPrinterPilot();const body=await request.json() as {email?:string;password?:string},token=await getLaboratoryBusinessRuntime().businessIdentity.login(body.email??"",body.password??"");const response=NextResponse.json({authenticated:true});response.cookies.set(BUSINESS_SESSION_COOKIE,token,{httpOnly:true,sameSite:"strict",path:"/",maxAge:28800});return response}catch{return NextResponse.json({error:"invalid_credentials"},{status:401})}}
export async function DELETE(){const response=NextResponse.json({authenticated:false});response.cookies.set(BUSINESS_SESSION_COOKIE,"",{httpOnly:true,expires:new Date(0),path:"/",sameSite:"strict"});return response}
