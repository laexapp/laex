import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, BrainCircuit, CheckCircle2, Gauge, History, ShieldAlert, Sparkles, TrendingUp } from "lucide-react";
import { findNewsEvent, newsEvents } from "@/modules/news-intelligence/infrastructure/demo-events";
export function generateStaticParams(){return newsEvents.map(event=>({slug:event.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const event=findNewsEvent(slug);return event?{title:`${event.title} | LAEX News Intelligence`,description:event.summary}:{};}
export default async function Detail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const e=findNewsEvent(slug);if(!e)notFound();return <main className="mx-auto w-[min(100%-2rem,78rem)] py-10 lg:py-16"><Link href="/noticias" className="inline-flex items-center gap-2 text-sm text-slate-500 hover:text-cyan-200"><ArrowLeft size={15}/> Volver al centro</Link><article className="mt-8"><div className="text-[10px] font-bold uppercase tracking-[.2em] text-cyan-300">{e.category} · {e.sector}</div><h1 className="laex-display mt-7 max-w-5xl text-5xl sm:text-7xl">{e.title}</h1><p className="mt-7 max-w-4xl text-lg leading-8 text-slate-300">{e.summary}</p><div className="mt-7 flex flex-wrap gap-2">{e.sources.map(s=><span key={s.name} className="rounded-full border border-white/10 px-4 py-2 text-xs text-slate-400">{s.name}</span>)}</div></article><section className="mt-12 grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><article className="rounded-3xl border border-cyan-300/20 bg-gradient-to-br from-cyan-300/[.08] to-blue-400/[.03] p-8"><div className="flex items-center gap-3 text-cyan-200"><BrainCircuit/><b className="text-xs uppercase tracking-[.2em]">¿Qué significa esto?</b></div><p className="mt-6 text-xl leading-9">{e.intelligence.meaning}</p><div className="mt-8 border-t border-white/[.08] pt-6"><p className="text-[10px] font-bold uppercase tracking-[.2em] text-slate-500">Interpretación LAEX · no es un hecho</p><p className="mt-3 text-sm leading-7 text-slate-300">{e.intelligence.interpretation}</p></div></article><aside className="laex-surface rounded-3xl p-7"><Gauge className="text-violet-300"/><p className="mt-5 text-xs uppercase tracking-[.18em] text-slate-500">Impacto estimado</p><strong className="mt-2 block text-4xl">{e.intelligence.impact}</strong><p className="mt-2 text-sm text-slate-500">Sentimiento {e.intelligence.sentiment.toLowerCase()}</p><div className="mt-8 flex justify-between text-xs"><span>Confianza</span><b>{e.intelligence.confidence}%</b></div><div className="mt-3 h-2 rounded-full bg-white/[.06]"><div className="h-full rounded-full bg-gradient-to-r from-cyan-400 to-violet-400" style={{width:`${e.intelligence.confidence}%`}}/></div></aside></section><section className="mt-5 grid gap-5 lg:grid-cols-3"><Info icon={<CheckCircle2 className="text-emerald-300"/>} title="Hechos confirmados"><ul className="space-y-3">{e.intelligence.facts.map(f=><li key={f} className="text-sm leading-6 text-slate-400">• {f}</li>)}</ul></Info><Info icon={<ShieldAlert className="text-orange-300"/>} title="Riesgo"><p>{e.intelligence.risk}</p></Info><Info icon={<Sparkles className="text-violet-300"/>} title="Oportunidad"><p>{e.intelligence.opportunity}</p></Info></section>{e.assets.length>0&&<section className="mt-14"><h2 className="flex items-center gap-3 text-2xl font-semibold"><TrendingUp className="text-cyan-300"/> Mercado relacionado</h2><div className="mt-5 grid gap-4 sm:grid-cols-2">{e.assets.map(a=><Link key={a.symbol} href={`/market/${a.slug}`} className="laex-surface flex items-center justify-between rounded-2xl p-6"><div><small className="text-slate-500">{a.symbol}</small><h3>{a.name}</h3></div><div className="text-right text-emerald-300">+{a.change}%<p className="text-xs text-slate-500">Vol. +{a.volumeChange}%</p></div></Link>)}</div></section>}<section className="mt-14 grid gap-6 lg:grid-cols-2"><article><h2 className="flex items-center gap-3 text-2xl font-semibold"><History className="text-violet-300"/> Historial del tema</h2><ol className="mt-6 border-l border-white/10 pl-6">{e.timeline.map(t=><li key={t.date} className="pb-6"><time className="font-mono text-xs text-cyan-300">{t.date}</time><p className="mt-2 text-sm text-slate-400">{t.label}</p></li>)}</ol></article><article className="laex-surface rounded-3xl p-7"><h2 className="text-xl font-semibold">Procedencia y agrupación</h2><p className="mt-4 text-sm leading-7 text-slate-400">{e.sources.length} {e.sources.length===1?"fuente reporta":"medios reportan"} este acontecimiento. LAEX conserva cada referencia y presenta una sola ficha para evitar duplicados.</p><p className="mt-5 rounded-xl bg-black/20 p-4 text-xs leading-6 text-slate-500">Datos demostrativos para validar el producto; no se presentan como información en tiempo real.</p></article></section></main>}
function Info({icon,title,children}:{icon:React.ReactNode,title:string,children:React.ReactNode}){return <article className="laex-card rounded-3xl p-7">{icon}<h2 className="mt-5 text-xl font-semibold">{title}</h2><div className="mt-5 text-sm leading-7 text-slate-400">{children}</div></article>}
