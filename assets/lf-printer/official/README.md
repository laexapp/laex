# Official LF-PRINTER media source

Drop approved source exports into the matching folder. Runtime files use the
same structure under `public/assets/lf-printer/official`.

The primary logo filename is `logos/lf-printer-logo-primary.svg`.
Printer hero filenames follow `printers/{slug}-hero.webp`.
Replacing an approved file with the same name does not require component edits.
